/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ['picsum.photos'], // Add the domain here
    },
};

export default nextConfig;
